<%= form_for(@thing) do |f| %>
  <% if @thing.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@thing.errors.count, "error") %> prohibited this thing from being saved:</h2>

      <ul>
      <% @thing.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  
  <h2 style="margin-bottom:30px;">FIELDS</h2>
  
  <div id="pieces">
    <%= render 'piece' %>
  </div>

  <button type="button" class="btn btn-primary" style="margin-top:20px;" id="add-piece">Add Another Field</button>
  
  <div class="actions" style="margin-top:30px;">
    <%= f.submit %>
  </div>
<% end %>
