<%= form_for(@thing) do |f| %>
  <% if @thing.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@thing.errors.count, "error") %> prohibited this thing from being saved:</h2>

      <ul>
      <% @thing.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  
  <%= render :partial => "piece_wrapper", locals: {:p_id => @p_id, :parent_p_id => @parent_p_id, :p_type => @p_type} %>

  <div class="actions" style="margin-top:30px;">
    
  </div>
<% end %>

<%= render 'thing_inputs' %>  

<button id="create_thing">
    <i class="fa fa-spin fa-refresh" id="create_thing_spinner" style="display:none;"></i>
    Create Thing
</button>
